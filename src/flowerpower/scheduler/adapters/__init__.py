# This file makes the 'adapters' directory a Python package.

# Optionally, expose specific adapters or the base class here
# from .base import BaseSchedulerAdapter
# from .apscheduler import APSchedulerAdapter

# For now, keep it simple and allow direct imports from submodules.