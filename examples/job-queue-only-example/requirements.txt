# Core FlowerPower dependencies (job queue only)
redis>=4.5.0
rq>=1.15.0
msgspec>=0.18.0

# No Hamilton dependencies required
# No pipeline dependencies required
# No fsspec required (unless using remote storage)

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0